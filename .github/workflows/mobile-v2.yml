name: UI Change Detector Mobile (Android Emulator)

on:
  workflow_dispatch:
    inputs:
      run_detector:
        description: "UI Change Detector Mobile (Android Emulator)"
        type: boolean
        default: true
  push:
    branches:
      - fix/maestro-v2
      - fix/maestro-v2-round2
    paths:
      - '.github/workflows/mobile-v2.yml'
      - 'scripts/run-mobile-v2-*.sh'
      - 'scripts/generate_detector_excel.py'
      - 'mobile-flows-v2/**'
      - 'exploration-2026-04-12/change-detector/**'
      - '.v2-trigger'

permissions:
  contents: read

jobs:
  mobile:
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.run_detector }}
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve run mode
        id: mode
        shell: bash
        run: |
          # workflow_dispatch (checkbox) -> qa-check; push -> read .v2-trigger or default "discovery"
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            RAW_MODE="qa-check"
          else
            RAW_MODE=""
            if [[ -f .v2-trigger ]]; then
              RAW_MODE=$(tr -d '[:space:]' < .v2-trigger)
            fi
          fi
          case "$RAW_MODE" in
            discovery) ;;
            test) ;;
            qa-check) ;;
            *) RAW_MODE="discovery" ;;
          esac
          echo "value=$RAW_MODE" >> "$GITHUB_OUTPUT"
          echo "Resolved mode: $RAW_MODE"

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Validate mobile secrets
        shell: bash
        env:
          MAESTRO_APP_EMAIL: ${{ secrets.HYDROCERT_MOBILE_QA_EMAIL }}
          MAESTRO_APP_PASSWORD: ${{ secrets.HYDROCERT_MOBILE_QA_PASSWORD }}
        run: |
          if [[ -z "${MAESTRO_APP_EMAIL}" || -z "${MAESTRO_APP_PASSWORD}" ]]; then
            echo "::error::Missing HYDROCERT_MOBILE_QA_EMAIL or HYDROCERT_MOBILE_QA_PASSWORD secret"
            exit 1
          fi
          echo "Mobile secrets present"

      - name: Install Excel dependency
        run: python3 -m pip install --upgrade pip openpyxl Pillow

      - name: Run mobile V2 (${{ steps.mode.outputs.value }}) on Android emulator
        id: mobile
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: default
          arch: x86_64
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim
          disable-animations: true
          emulator-boot-timeout: 600
          script: bash $GITHUB_WORKSPACE/scripts/run-mobile-v2-${{ steps.mode.outputs.value }}.sh
        env:
          GH_TOKEN: ${{ github.token }}
          MAESTRO_APP_EMAIL: ${{ secrets.HYDROCERT_MOBILE_QA_EMAIL }}
          MAESTRO_APP_PASSWORD: ${{ secrets.HYDROCERT_MOBILE_QA_PASSWORD }}

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-v2-${{ steps.mode.outputs.value }}-${{ github.run_id }}
          path: |
            qa-artifacts/mobile-v2/
            qa-artifacts/change-detector/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Excel report
        if: always() && steps.mobile.outputs.excel_path != ''
        id: upload_excel
        uses: actions/upload-artifact@v4
        with:
          name: UIChangeDetector-Excel-${{ github.run_id }}
          path: ${{ steps.mobile.outputs.excel_path }}
          if-no-files-found: ignore
          retention-days: 14

      - name: Publish workflow summary
        if: always()
        shell: bash
        run: |
          MODE="${{ steps.mode.outputs.value }}"
          {
            if [[ "$MODE" == "qa-check" ]]; then
              echo "## UI Change Detector Mobile (Android Emulator)"
              echo ""
              DETECTOR_RESULT=$(ls -1t qa-artifacts/change-detector/scan_results_*.json 2>/dev/null | head -1)
              if [[ -n "$DETECTOR_RESULT" ]]; then
                node -e "
                  const s = require('./$DETECTOR_RESULT');
                  const sum = s.summary || {};
                  const screens = s.screens || {};
                  const scanned = sum.screens_scanned || 0;
                  const newEls = sum.total_new_elements || 0;
                  const passed = Object.values(screens).filter(v => (v.new_element_count || 0) === 0).length;
                  const failed = scanned - passed;
                  console.log('| Metric | Value |');
                  console.log('|--------|-------|');
                  console.log('| Screens scanned | ' + scanned + ' |');
                  console.log('| Passed (no changes) | ' + passed + ' |');
                  console.log('| Failed (new elements) | ' + failed + ' |');
                  console.log('| Total new elements | ' + newEls + ' |');
                  if (newEls > 0) {
                    console.log('');
                    console.log('### New UI elements detected');
                    console.log('');
                    for (const [name, info] of Object.entries(screens)) {
                      const els = info.new_elements || [];
                      if (els.length > 0) {
                        console.log('**' + name + '** (' + els.length + ' new):');
                        for (const el of els) {
                          const label = el.text || el.content_desc || el.resource_id || 'unlabeled';
                          console.log('  - ' + label);
                        }
                      }
                    }
                  } else {
                    console.log('');
                    console.log('All screens match baseline. No new elements detected.');
                  }
                " 2>/dev/null || echo "Summary parse failed"
              else
                echo "No scan results found — scanner may have failed. Check logs."
              fi
              echo ""
              EXCEL_FILE=$(ls -1t qa-artifacts/change-detector/UIChangeDetector_Mobile_*.xlsx 2>/dev/null | head -1)
              if [[ -n "$EXCEL_FILE" ]]; then
                echo "**Excel report:** \`$(basename "$EXCEL_FILE")\`"
                echo ""
                echo "[Download Excel report](${{ steps.upload_excel.outputs.artifact-url || '#' }})"
              fi
            else
              echo "## Mobile V2 ($MODE)"
              echo ""
              SUMMARY_FILE="qa-artifacts/mobile-v2/$MODE/summary.json"
              if [[ -f "$SUMMARY_FILE" ]]; then
                node -e "const s=require('./$SUMMARY_FILE'); console.log('- Mode: ' + (s.mode || '$MODE')); console.log('- Total: ' + s.totals.total); console.log('- Passed: ' + s.totals.pass); console.log('- Failed: ' + s.totals.fail); console.log('- Skipped: ' + s.totals.skip);" || echo "Summary parse failed"
              else
                echo "No summary file at $SUMMARY_FILE"
              fi
            fi
            echo ""
            echo "---"
            echo "Artifact: mobile-v2-$MODE-${{ github.run_id }}"
          } >> "$GITHUB_STEP_SUMMARY"
