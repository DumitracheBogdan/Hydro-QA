appId: com.hydrocert.app
name: "MOB27 V2 - Start inspection form navigation"
tags:
  - inspections
  - forms
  - smoke
---

# Opens the QA test visit (has 4 inspections), switches to
# Inspections tab, taps "Start Inspection" on the first card,
# verifies the form opened.

# CIRCLE: Visit Information, Risk Assessment, Actions, Submit
- runFlow: _shared/open_qa_test_visit.yaml

# --- Switch to Inspections tab ---
- tapOn:
    text: "Inspections.*"

# --- Wait for inspection list ---
- extendedWaitUntil:
    visible: "Start Inspection"
    timeout: 10000

# --- Start first inspection ---
- tapOn:
    text: "Start Inspection"
    index: 0

# --- Verify inspection form opened ---
- extendedWaitUntil:
    visible: "(Visit Information|Submit)"
    timeout: 10000
