appId: com.hydrocert.app
name: "MOB21 V2 - Priority picker dropdown"
tags:
  - visit-detail
  - priority
  - actions
  - smoke
---

# Opens the "QA test" visit (which has action items with
# priorities), finds a priority badge, taps it to open the
# picker dropdown, verifies all options, dismisses.
# Source: exploration test 12_visit_detail_priority_picker.yaml
#
# NOTE: Uses Visits Home "View Visit Details" (first card = QA test)
# instead of History because only QA test visit has action items.

- runFlow: _shared/login.yaml

# --- Open first visit from Visits Home (QA test - has actions) ---
- tapOn: "View Visit Details"
- extendedWaitUntil:
    visible: "Visit Details"
    timeout: 15000

# --- Scroll down to find a priority badge in Actions area ---
- scrollUntilVisible:
    element:
      text: "(Low|Medium|High)"
    direction: DOWN
    timeout: 15000

# --- Tap the priority badge to open dropdown ---
- tapOn:
    text: "(Low|Medium|High)"

# --- Verify all three priority options visible ---
- extendedWaitUntil:
    visible: "Medium"
    timeout: 10000
- assertVisible: "Low"
- assertVisible: "Medium"
- assertVisible: "High"

# --- Dismiss picker without changing ---
- back

# --- Navigate back ---
- back
