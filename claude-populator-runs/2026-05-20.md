# Claude Populator Daily Run — 2026-05-20

**Status:** ABORTED — environment cannot reach Hydrocert dev API.

## Run metadata

- Date (UTC): 2026-05-20
- Model: claude-opus-4-7
- Entrypoint: remote_trigger (Claude Code on the Web, cloud_default environment)
- Session: `cse_01BpkPfgjXEfdtESBCzDBRNc`
- Duration: ~1 minute (aborted at auth step)

## Summary stats

| Metric | Value |
|---|---|
| visitsScanned | 0 |
| visitsPatched | 0 |
| totalInspectionsPatched | 0 |
| skippedAlreadySampled | 0 |
| skippedNoSampleJt | 0 |
| skippedPreSync | 0 |
| skippedComplete | 0 |
| unresolved | 0 |
| errors | 1 (network) |

## Blocker

Step 1 of the runbook (`POST /auth/login`) failed with **HTTP 403** before reaching
the Hydrocert auth handler. The response was synthesized by this environment's
egress gateway:

```
HTTP/2 403
x-deny-reason: host_not_allowed
content-type: text/plain

Host not in allowlist
```

The TLS handshake reaches the real Azure backend (cert CN
`*.ukwest-01.azurewebsites.net` validates), so DNS and routing work — the
container's egress proxy is what rejects the request.

Probed the following hosts; all denied with the same `host_not_allowed` header:

| Host | Result |
|---|---|
| `hydrocert-dev-api-exajhpd0brg2bcar.ukwest-01.azurewebsites.net` | 403 host_not_allowed |
| `dev.gen-cert.com` | 403 host_not_allowed |
| `gen-cert.com` | 403 host_not_allowed |
| `hydrocert-prod-api.azurewebsites.net` | 403 host_not_allowed (expected; not used) |
| `api.github.com` | 403 (direct), but git/MCP routes work via local proxy |
| `github.com` | 200 |

No outbound HTTP proxy env var is set; `CLAUDE_CODE_PROXY_RESOLVES_HOSTS=true`
is the only proxy-related hint and it does not override the allowlist.

## What this means

The populator routine cannot run from this environment until the dev API
host is added to the network policy for sessions started by this trigger.

- No visits were scanned.
- No inspections were patched.
- No state on the Hydrocert dev tenant was changed (this run is a strict no-op).
- Idempotency is preserved trivially.

## Suggested fix

Add the dev API host to the environment's outbound allowlist:

- `hydrocert-dev-api-exajhpd0brg2bcar.ukwest-01.azurewebsites.net`
- (Optional, only if deep-link verification is wanted) `dev.gen-cert.com`

Network policies for Claude Code on the Web environments are documented at
<https://code.claude.com/docs/en/claude-code-on-the-web>. The environment
that triggers this routine should select a policy (or custom allowlist)
that includes the dev API host.

**Do not** add the prod host (`hydrocert-prod-api.azurewebsites.net`) — the
runbook's guardrails forbid touching prod.

## Per-visit table

_None — visit listing was never reached._

## Unresolved details

_None._

## Errors

1. `POST /auth/login` → `403 host_not_allowed` (egress gateway). Run aborted
   per the "On auth failure … abort with error" guardrail.

## Decisions audit log

1. Attempted `POST /auth/login` against the documented dev API URL → 403 from proxy.
2. Probed sibling hosts to confirm the denial was policy-level, not credential-level.
3. Inspected environment variables, listening ports, and git remote config for any
   pre-configured tunnel or sidecar. None found (the only local proxy is the git
   proxy on `127.0.0.1:40181`, which only handles git/MCP traffic to GitHub).
4. Did not retry or attempt to bypass the allowlist. Wrote this log and opened
   a tracking GitHub issue per the runbook's reporting step.
