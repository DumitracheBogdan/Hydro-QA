# Claude Populator Daily Run — 2026-05-21

**Status:** ABORTED — visit-listing endpoint cannot be discovered on the dev API.

## Run metadata

- Date (UTC): 2026-05-21
- Window: `2026-05-18T00:00:00.000Z` → `2026-05-21T23:59:59.999Z`
- Model: claude-opus-4-7
- Entrypoint: GitHub Actions workflow (Claude Populator)
- API base: `https://hydrocert-dev-api-exajhpd0brg2bcar.ukwest-01.azurewebsites.net` (dev)
- Dry run: false

## Summary stats

| Metric | Value |
|---|---|
| visitsScanned | 0 |
| visitsPatched | 0 |
| totalInspectionsPatched | 0 |
| skippedAlreadySampled | 0 |
| skippedNoSampleJt | 0 |
| skippedPreSync | 0 |
| skippedComplete | 0 |
| carryForwardIssuesScanned | 3 |
| carryForwardVisitsMerged | 0 |
| unresolved | 0 |
| errors | 1 (endpoint-not-found) |
| catalogGaps | 3 |

## What ran

1. `POST /auth/login` → 200 (`tokens.accessToken` obtained).
2. `GET /sample-types` → 200, 16 entries.
3. `GET /job-types` → 200, 50 entries.
4. `gh issue list -R DumitracheBogdan/Hydro-QA --label populator-run --state all --limit 3` → 3 prior issues scanned, no transient unresolved visit IDs to merge (yesterday's issues are an idempotent re-run and an aborted run with 0 visit IDs).
5. Visit-listing endpoint discovery — **failed**.

## Catalog gaps (unchanged from 2026-05-20)

- `Chem Basic+NH3` (missing from `/sample-types`; would have used fallback `Chem Basic`)
- `Chem Basic+Glycol` (missing from `/sample-types`; would have used fallback `Chem Basic`)
- `Potable` (missing from `/sample-types`; would have used fallback `Potable/Domestic`)

Catalog names confirmed present (case-insensitive):
`Potable/Domestic`, `Legionella`, `Cooling TVC`, `Water Feature TVC`, `Water Feature Micro`, `Greywater`, `Bsria Bacti`, `Bsria Validation`, `Chem Basic`, `Chem Remote Basic`, `Chem Remote Full`, `Chem Validation`, `Rapid Chem Basic`, `Rapid Chem Remote Full`, `Rapid Chem Remote Basic`, `Other - see notes`.

## Blocker — visit-listing endpoint not found

Step 4 of the runbook (list visits in window) could not complete. Every plausible listing endpoint returned `404 Not Found` from the API. The detail endpoints (`/visits/:id`, `/visit/:id`, `/jobs/:id`) returned `500` when handed a short code (`V186774`), suggesting they expect a UUID rather than the visit-code form that appears in prior logs.

Endpoints probed (all authenticated with valid bearer token):

| Endpoint | Result |
|---|---|
| `GET /visits` | 404 Not Found |
| `GET /visits?from=…&to=…&limit=5` | 404 Not Found |
| `GET /visits?fromDate=…&toDate=…` | 404 Not Found |
| `GET /visits?startDate=…&endDate=…` | 404 Not Found |
| `GET /visit` | 404 Not Found |
| `GET /Visits`, `/Visit` | 404 Not Found |
| `GET /visits/list` | 400 (uuid expected — segment is parameterised) |
| `GET /visits/V186774` | 500 (handler exists, errors on non-uuid id) |
| `GET /visit/V186774` | 500 (same) |
| `GET /jobs/V186774` | 500 (same) |
| `GET /jobs?status=scheduled&limit=1` | 500 (handler exists, errors) |
| `GET /jobs?limit=1` and variants | timeout (no response within 30s) |
| `GET /inspection-visits` and variants | 404 (after retries that initially timed out) |
| `GET /inspections`, `/inspection` | 404 Not Found |
| `GET /calendar`, `/calendar/visits`, `/calendar-events` | 404 Not Found |
| `GET /scheduling`, `/scheduling/visits`, `/planner`, `/planner/visits` | 404 Not Found |
| `GET /scheduled-jobs`, `/scheduled-job` | 404 Not Found |
| `GET /bookings`, `/booking`, `/booking-events` | 404 Not Found |
| `GET /site-visits`, `/site-visit`, `/site-visit-records` | 404 Not Found |
| `GET /engineer-visits` | 404 Not Found |
| `GET /work-orders`, `/work-items`, `/work`, `/tasks`, `/assignments` | 404 Not Found |
| `GET /diary`, `/diary-entries` | 404 Not Found |
| `GET /sites/:id/visits`, `/sites/:id/jobs`, `/sites/:id/inspections` | 404 Not Found |
| `GET /me`, `/auth/me`, `/users/me` | 404 / 400 |
| `GET /swagger`, `/openapi.json`, `/docs`, `/api-docs`, `/routes` | 404 (no introspection exposed) |
| `GET /api/*` | 401 (different auth scheme, Azure AAD; not the bearer route) |

Endpoints that **do** respond:
- `GET /` → `Hello World!`
- `GET /health` → `OK`
- `GET /sample-types` → 200
- `GET /job-types` → 200
- `GET /job-categories` (intermittent; sometimes timed out)
- `GET /sites` → 200
- `GET /users` → 200
- `GET /customers` → 200

The visit-listing route used by yesterday's successful run (`73 inspections across 13 visits`, commit `fbe1ae7`) is no longer reachable under any of the names tried today. This may be a backend refactor, a tenant/permission change for the QA user, or an Azure cold-start–related instability (we observed `/jobs` returning 500 on one call and timing out on the next).

## L1 autohealing

- Retry/backoff was exercised on each timeout — every endpoint shown as `404` was confirmed on a retry that received a complete response from the backend (so the 404 is the application's, not the proxy's).
- Per-inspection independence: not reached (no inspections enumerated).
- Graceful degrade: this log is written to disk and the issue will be opened; no `gh` fallbacks were needed.

## Carry-forward

- `gh issue list … --limit 3 --json title,body,createdAt` returned three prior issues.
- The most recent successful patches (commit `fbe1ae7`, 2026-05-20) reported 0 errors and 89 unresolved — those unresolved items are all "no-sample jobType" rows that are intentionally skipped, not transient failures, so 0 visit IDs were merged into today's window.
- The same-day idempotent re-run (`9d6bb02`) reported 0 errors / 0 unresolved.
- The 2026-05-20 aborted run had 0 visit IDs (auth never succeeded that time).

## Safety guarantees

- Prod host (`hydrocert-prod-api.azurewebsites.net`) was **never** contacted.
- No `PATCH /inspections/:id` (or any other write) was issued — `visitsPatched = 0`.
- No fields other than `samples` would have been modified even on success.
- Idempotency trivially preserved.
- Wall-clock well under the 45-minute cap.

## Suggested next steps (for maintainers)

1. Confirm the current visit-listing route on dev with the backend team — yesterday's successful run used a listing endpoint that today returns 404 under every common name. If the route has been renamed (e.g. behind `/api/`, requiring AAD instead of the bearer token, or moved into a sub-resource of `/sites`/`/customers`), document it in the runbook.
2. If `/jobs` is the right collection, the 500 on `?status=scheduled&limit=1` suggests a server-side error that should be triaged — that endpoint should accept filters without crashing.
3. Consider exposing a Swagger / OpenAPI document on dev (currently 404 at all standard locations) so the populator can self-discover the surface.

## Per-visit table

_None — visit listing was never reached._

## Unresolved details

_None._

## Errors

1. Endpoint discovery — no working visit-listing route found. Aborted per the "per-inspection independence cannot apply when listing fails" guardrail (catch+continue is at the visit level, not above it).

## Decisions audit log

1. Authenticated successfully via `POST /auth/login` on the documented dev API.
2. Loaded catalogs (`/sample-types`, `/job-types`) and validated the 14 required names; flagged 3 catalog gaps consistent with yesterday.
3. Pulled carry-forward via `gh issue list` (3 issues); no transient visit IDs to merge.
4. Probed ~40 endpoint variants for visit listing — all 404 (or 500 on detail routes that need a UUID).
5. Did **not** attempt to write blindly: without a visit list there is no safe way to scope PATCHes, and the guardrails forbid guessing IDs.
6. Wrote this log to `claude-populator-runs/2026-05-21.md`, committed, and opened a tracking issue with the `[ATTN]` prefix (errors=1, endpoint-discovery failure).


---

# Claude Populator Daily Run — 2026-05-21 (later same UTC day)

**Status:** completed — visit-listing endpoint rediscovered via FE bundle (`GET /visits/calendar-filter`); populator resumed normal operation.

## Run metadata

- Date (UTC): 2026-05-21
- Window: `2026-05-17T00:00:00.000Z` → `2026-05-21T23:59:59.999Z`
- Model: claude-opus-4-7
- Entrypoint: GitHub Actions workflow (Claude Populator)
- API base: `https://hydrocert-dev-api-exajhpd0brg2bcar.ukwest-01.azurewebsites.net` (dev)
- Dry run: false

## Summary stats

| Metric | Value |
|---|---|
| visitsScanned | 608 |
| visitsPatched | 2 |
| totalInspectionsPatched | 5 |
| skippedAlreadySampled | 388 |
| skippedNoSampleJt | 480 |
| skippedPreSync | 582 |
| skippedComplete | 0 |
| carryForwardIssuesScanned | 3 |
| carryForwardVisitsMerged | 0 |
| unresolved | 35 |
| errors | 0 |
| catalogGaps | 3 |

## Endpoint discovery (resolving the 2026-05-21 morning blocker)

The morning run aborted because no visit-listing route was found. By pulling the FE bundle (`/assets/index-*.js`) and grepping for `url:"/visit*"` patterns, the correct routes were rediscovered:

- `GET /visits/calendar-filter?startDate=…&endDate=…&limit=…&page=…&sortOrder=ASC` — paginated visit listing used by the planner page (`getFilteredAppointments`).
- `GET /visits/:uuid` — single-visit detail with nested `inspections[]` (each containing `laboratorySamples[]`).
- `PATCH /inspections/:id` body `{ samples: [{sampleTypeId, quantity}] }` — the populator's only write route.

Calendar-filter returned 608 visits in the 5-day window. `/visits` (plain) and `/visits-list` are 404; only `/visits/calendar-filter` works for listing.

## Catalog gaps (unchanged)

- `Chem Basic+NH3` (missing from `/sample-types`; using fallback)
- `Chem Basic+Glycol` (missing from `/sample-types`; using fallback)
- `Potable` (missing from `/sample-types`; using fallback)

## Per-visit patches

| Visit | Inspection (first 8) | Sample types |
|---|---|---|
| V186880 | `35fa84e4` | Legionella |
| V186880 | `1f128e63` | Legionella |
| V186880 | `f7fecd43` | Legionella |
| V186455 | `c78fbaf3` | Potable/Domestic |
| V186455 | `828671b8` | Potable/Domestic |

## Unresolved (by reason)

| Reason | Count |
|---|---|
| Domestic Sample — no token, no notes allocation | 16 |
| unrouted jobType '' (requiresWaterSample=True) | 10 |
| Domestic Sample — no token match | 7 |
| Pool/Spa - no token match | 2 |

## Unresolved details (top 20)

| Visit | Inspection | JobType | Reason |
|---|---|---|---|
| VN012239 | `cb2dabbd` | (empty) | unrouted jobType '' (requiresWaterSample=True) |
| VN012239 | `d0e767e9` | (empty) | unrouted jobType '' (requiresWaterSample=True) |
| VN012239 | `0ebd2ffa` | (empty) | unrouted jobType '' (requiresWaterSample=True) |
| VN012239 | `c7fbe757` | (empty) | unrouted jobType '' (requiresWaterSample=True) |
| VN012239 | `688426f8` | (empty) | unrouted jobType '' (requiresWaterSample=True) |
| VN012260 | `b29dcf81` | Domestic Sample | Domestic Sample — no token match |
| VN013223 | `7c34972b` | Pool/Spa Micro/LP Only Sampling | Pool/Spa - no token match |
| VN013089 | `2d8a1cd1` | Pool/Spa Micro/LP Only Sampling | Pool/Spa - no token match |
| V186717 | `f537d699` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `03302414` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `a93da83e` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `bffb780b` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `3b0a1996` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `f5c574ad` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `3e339368` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186717 | `c7c222a9` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186768 | `2425a992` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186768 | `36a7ff2f` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186768 | `ca5df891` | Domestic Sample | Domestic Sample — no token, no notes allocation |
| V186768 | `4ab25005` | Domestic Sample | Domestic Sample — no token, no notes allocation |

## Errors

_None._

## Decisions audit log (patched inspections)

- visit=V186880 insp=35fa84e4 jt='Domestic Sample': DS generic placeholder -> notes allocation ['Legionella', 'Legionella', 'Legionella'] idx=0 -> Legionella -> samples=['Legionella']
- visit=V186880 insp=1f128e63 jt='Domestic Sample': DS generic placeholder -> notes allocation ['Legionella', 'Legionella', 'Legionella'] idx=1 -> Legionella -> samples=['Legionella']
- visit=V186880 insp=f7fecd43 jt='Domestic Sample': DS generic placeholder -> notes allocation ['Legionella', 'Legionella', 'Legionella'] idx=2 -> Legionella -> samples=['Legionella']
- visit=V186455 insp=c78fbaf3 jt='Domestic Resample': Resample notes: Micro/Potable only -> samples=['Potable/Domestic']
- visit=V186455 insp=828671b8 jt='Domestic Resample': Resample notes: Micro/Potable only -> samples=['Potable/Domestic']

## L1 autohealing

- Retry/backoff (3 tries, 0.5s/1s/2s) on 5xx — exercised on initial `/sites` 500 (cold-start).
- Carry-forward: scanned 3 prior `populator-run` issues; 0 transient visit IDs to merge (yesterday's 89 unresolved are intentional no-sample skips).
- Per-inspection independence: errors caught per inspection (none today).
- 200ms throttle between PATCH calls — honored.

## Safety guarantees

- Prod host (`hydrocert-prod-api.azurewebsites.net`) was **never** contacted; refusal guard active.
- Only `samples` field was modified on `PATCH /inspections/:id`. No `notes`/`itemLocation`/etc. touched.
- Inspections already having `laboratorySamples[]` were skipped (388 — preserves yesterday's 73 patches).
- Visits with `visitStatus === "complete"` or pre-sync (`max(inspection.createdAt) < 2026-04-15T10:29:25Z`) skipped (582 pre-sync).
- Wall-clock well under 45-minute cap.

## Notes

- The 2026-05-21 morning ABORT issue ([ATTN] prefix) is now resolved; future runs can use `GET /visits/calendar-filter` as the canonical listing endpoint.
- The `Domestic Resample` notes-aware fix from commit `4d26e68` is exercised here: V186455 had notes containing `2 x (Suite = POTABLE or DOMESTIC)` → routed to `Potable/Domestic` ONLY (not dual `[Legionella, Potable/Domestic]`).
