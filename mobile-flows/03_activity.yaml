appId: com.hydrocert.app
name: "Activity Screen"
tags:
  - activity
  - smoke
---



# =============================================
# ACTIVITY SCREEN
# =============================================

- launchApp:
    clearState: true

# --- Always do login (fresh app state) ---
- extendedWaitUntil:
    visible: "Welcome.*"
    timeout: 30000
- tapOn: "Email"
- inputText: "${MAESTRO_APP_EMAIL}"
- hideKeyboard
- tapOn:
    text: "Password"
- inputText: "${MAESTRO_APP_PASSWORD}"
- hideKeyboard
- tapOn: "Login"
- extendedWaitUntil:
    visible: "overview of your visits"
    timeout: 60000

# --- Navigate to Activity tab ---
- tapOn:
    text: "Activity"

# --- Verify Activity Screen ---
- assertVisible: "No activities found."

# --- Profile Avatar (top-right, clickable) ---

# --- Bottom Navigation ---
- assertVisible: "Visits"
- assertVisible: "History"
- assertVisible: "Activity"
- assertVisible: "Account"

